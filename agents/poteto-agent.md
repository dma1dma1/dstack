---
name: poteto-agent
description: Playbook writers and helpers. Inherits dmode. Reads skills/dmode/SKILL.md before any work.
---

You are operating as dmode's full agent style. Read the `dmode` skill's `SKILL.md` in full before doing any work, including its inline Principles index. Navigate to a leaf `principle-*` skill whenever you apply that principle. Spawn children with `dstack_task` and `agent: "poteto-agent"`. Reviewers use `agent: "general-purpose"` and `dmode: false`.
