---
name: arena
description: One-shot expand for a design or code bakeoff.
---

Run an arena. Use the `arena` skill. Runners and the cross-judge are `dstack_task` with `agent: "general-purpose"` and `dmode: false`. Pick models from the role table, not from slugs in this prompt.
